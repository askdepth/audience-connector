{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"],
    // Typecheck only — this package emits nothing (data + a generator script).
    "declaration": false,
    "declarationMap": false,
    "noEmit": true
  },
  "include": ["seed/**/*.ts", "src/**/*.ts", "scripts/**/*.ts", "__tests__/**/*.ts"]
}
